[Unit]
Description=Nova Executive - CEO
After=network.target nova-relay.service

[Service]
Type=simple
User=nova
WorkingDirectory=/opt/nova
ExecStart=/home/nova/.bun/bin/bun run --env-file=.env.ceo src/executive-node.ts --role ceo
Restart=always
RestartSec=10

Environment=HOME=/home/nova
Environment=PATH=/home/nova/.bun/bin:/usr/local/bin:/usr/bin:/bin

StandardOutput=journal
StandardError=journal
SyslogIdentifier=nova-exec-ceo

[Install]
WantedBy=multi-user.target
