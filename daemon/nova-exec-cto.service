[Unit]
Description=Nova Executive - CTO
After=network.target nova-relay.service

[Service]
Type=simple
User=nova
WorkingDirectory=/opt/nova
ExecStart=/home/nova/.bun/bin/bun run --env-file=.env.cto src/executive-node.ts --role cto
Restart=always
RestartSec=10

Environment=HOME=/home/nova
Environment=PATH=/home/nova/.bun/bin:/usr/local/bin:/usr/bin:/bin

StandardOutput=journal
StandardError=journal
SyslogIdentifier=nova-exec-cto

[Install]
WantedBy=multi-user.target
